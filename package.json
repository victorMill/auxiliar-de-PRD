{
  "name": "checklist-documentos-bnb",
  "version": "1.0.0",
  "description": "Verificador de documentos necessários para operações do BNB",
  "homepage": "https://milhomem.github.io/checklist_documentos_prd",
  "scripts": {
    "start": "npx http-server . -o",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d .",
    "build": "echo 'No build step needed'"
  },
  "author": "BNB",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
